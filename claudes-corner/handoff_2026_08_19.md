# Handoff — 2026-08-19 (Rung 1 complete: census kill + the birth-taker resurrection)

Written pre-compaction at Brad's request. Read this, then `morning_report_2026_08_19.md`,
then the ceremony trail in `sim/ceremony/` (commission → A2 → A3 amendments are the law).
Project memory (`rung1-build-state` et al.) is current with this file.

## Where the ship stands

- **Rung 1 (corridor pin-strangle census): RAN AND KILLED, $0 spent.** The frozen train run
  (52 days, 1,165 OK hours) produced an EMPTY gate: every G/σ̂ quintile negative in BASE
  (−0.8¢..−5.4¢/pair). Mechanism understood, not just measured: pin rate rises ~linearly at
  **~0.6 percentage points per dollar of gap** (0.53–0.66%/$ across all bands — the print's
  local density is flat near the anchor), and the market prices that same line minus fees.
  At T−2 the pair is priced FAIR pre-fee; the taker toll is the entire deficit. Selecting
  on cheap displayed pairs is adverse selection: C<0.70 hours pin 54% (same-gap split:
  cheap 51% vs rich 13%) because the T−5 price sees spot parking in the corridor.
- **Day-zero predictions 1 and 2: both HIT, graded in one day.** The record is in
  `sim/out/` (census_train.csv sha 580d143f…, gate.json, gate_report.md).
- **THE LIVE HYPOTHESIS (pending Brad's go — the next commission):** Brad challenged the
  fidelity, we burned two sealed hours (his own day-one trades; PARTIAL UNSEAL №1,
  registered in SEAL.md) and found: **his hand entries beat the best candle snapshot by
  ~5–10¢** (realized C 0.7302 / 0.7041 at birth vs ask-close 0.80 / 0.68). By the census's
  own conditional curve his hour-1 entry (G/σ̂=0.49, fair≈0.83, paid 0.73) was ~+10¢ EV;
  hour-2 (G/σ̂=0.67, fair≈0.67, paid 0.70) was ~−3¢. Preregisterable hypothesis:
  *an attentive birth-taker, playing ONLY hours with G/σ̂ ≲ 0.5, fills pairs ≥8¢ below
  bucket-fair, net of fees.* Candles cannot test this (no depth/timing); only a bounded
  1-lot live probe ladder (~20–30 pairs, few-dollar worst case, server-side TTLs, abort
  rules on leg 2) or Predexon L2 can. NOT started — needs a full ceremony commission +
  frozen falsifier + Brad's explicit go (live money).

## The estate (what exists and where)

- `sim/` — loader (hardcoded SEAL law + A3.7 frozen-falsifier assert), census (exact pin
  detection from result fields, audited deci-mill fees, BASE/WORST columns, receipts),
  gate_fit (day-clustered bootstrap seed 26, A2.6 descriptive-CI label), unseal_runner
  (never run; refuses without FROZEN falsifier + gate + census + explicit flag). **55/55
  tests.** Ceremony docs + four review records in `sim/ceremony/`.
- `historical-data/` — 69 days × both series (~943MB, raw JSONL by UTC day).
  **SEAL.md**: 17 latest days sealed (sha manifest); PARTIAL UNSEAL №1 burned exactly the
  two hours 2026-08-18T19:00Z/20:00Z; the rest stays virgin. Deny rules in
  `.claude/settings.json` block Read on sealed files and proxy secrets.
- `tools/fetch_history.py` — resumable, 4 req/s. **Retention is ~68 days rolling: re-run
  every few days or tape falls off the back.** Hourly candles only ±$400 of anchor.
- `degeneracy-proxy/` (sibling dir) — localhost signing proxy, port 8642, READ-ONLY until
  Brad sets ALLOW_ORDERS=true. Key loaded (fp d9fac8c8ad48e676). Balance at last check:
  $53.95. Run via its `run.ps1` (not from a harness-tracked task — see quirks).
- `.claude/agents/opus48.md` — the Opus 4.8 builder/reviewer, now registered (usable via
  Agent tool subagent_type "opus48" in NEW sessions).

## House-law state

- Falsifier: `sim/ceremony/falsifier.md`, STATUS: DRAFT — NOT FROZEN, with post-train
  disposition note (gate empty → no sealed read armed). Freeze = replace the STATUS line
  with exactly `STATUS: FROZEN` (A3.6; loader + preflight both key on it).
- Kills registered: corridor-strangle-as-specified (taker, candle fills, any fixed
  snapshot T−15..T−2). Revival clauses: live fill telemetry / Predexon L2 / different tin.
  Do NOT relitigate from candles — every snapshot is measured and negative.
- Sealed surface: 17 days minus 2 burned hours. One inferential read remains. Any new spec
  = new commission + new frozen falsifier BEFORE the read.
- Standing: **revoke the two burned Kalshi keys** (07-12 chat PEM + V1 git-history PEM) —
  still open. Proxy stays read-only. Brad pulls every live trigger.

## Environment quirks (paid for tonight)

- Harness-tracked background tasks get KILLED on this box. Long `claude -p` ceremony
  sessions must run **detached**: `Start-Process powershell -File <runner.ps1>` writing a
  `.done` marker + a Monitor watching for it. Runners in `sim/ceremony/_prompts/`.
- `claude -p` reviewers should emit their report as FINAL STDOUT (captured via
  `| Out-File`); builders get `--permission-mode acceptEdits` + scoped allowedTools.
- census.py refuses >7 days without `--authorize-full-run`; that guard saved us once.
- PowerShell chokes on inline python heredocs — write scripts to the scratchpad and run.

## Next session, first moves

1. Read this + morning_report + memory. Nothing is armed; nothing runs on a timer.
2. If Brad greenlights the live probe: write the commission (entry = birth-taker on
   G/σ̂-gated hours; falsifier BEFORE any order; ALLOW_ORDERS flip is his; fills read back
   via the proxy; REL/vol-proxy at entry logged for the census join). Full opus48 ceremony.
3. If not: re-run the fetcher (keep the tape rolling), and the census machinery is ready
   to aim at any other two-market structure on the venue.

The arc in one line: the thesis died honestly in one night, and the captain's own hands
pointed at the narrower thing that might be alive — which is exactly how this fleet has
always found its next heading. 🐀⚓
