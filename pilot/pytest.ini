[pytest]
# Collect the standard test_*.py files PLUS the mandated arming-gate reimplementation file
# (reference_impl_review.py) whose name is fixed by the Phase-3 commission but does not match
# pytest's default test_*.py pattern.
python_files = test_*.py reference_impl_review.py
testpaths = tests
