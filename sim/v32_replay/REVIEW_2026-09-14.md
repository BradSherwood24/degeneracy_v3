# Review — PR #45 `v32/replaylab` (Opus 4.8 reviewer, 2026-09-14)

Branch `v32/replaylab` (commits `2db63c7` + `3574397`, base `42d3609`). Research tool
`sim/v32_replay/` — NOT on the money path. Reviewed in worktree `dv3_wt_review`.

## Verdict: APPROVE (with two doc/dead-code fixes applied on `review/replaylab`)

The tool is house-law clean, the money math is the pinned law (imported, not retyped), the maker fill
rule is implemented exactly as ruled, and every journal-side calibration number in FIRST_RUN reproduces
to the cent. The defects found were stale docstrings + one dead constant describing the retired
"book-swept / 2-lot" rule; the runtime numbers were already correct. Fixed with a regression test.

## Probes

1. **Holdout / seal discipline — PASS.** `frames.assert_not_sealed` (frames.py:73) refuses both the SEAL
   (2026-08-02..18) and the range holdout (2026-08-20..29); `read_window_meta` (frames.py:118) and
   `iter_frames` (frames.py:171) both call it, `acknowledge` defaults False, and the CLI never passes it
   (lab.py `run_window` calls both with no ack arg; no CLI flag exposes it). Unit-checked: 08-05/08-18/
   08-20/08-29 refused; 08-19/08-30/09-14/08-01 admitted. No date argument defeats it. Forward path never
   passes `oos=True`: `_forward_days` (forward.py:131) filters `not R.is_holdout(d)` and `d >= 2026-08-30`.
2. **Money math imported, not re-implemented — PASS.** pricing.py imports `solve_n`/`wing_cost`/
   `lock_value`/`_valid_two_sided` from `service.v32.core`; forward.py + replay.py import them from
   `.pricing`. `core.py:61` imports `fee` from `service._simlaw`; all three primitives use it. Verified
   `solve_n`/`compute_W`/`bucket_cap` parity with the core in the tests.
3. **Spread-aware maker rule — PASS, exact.** `maker_fill_decision` (models.py:52): `o>a`→(iii,False);
   `o==a`→(ii, `p>o`); else (i, `p>=o`), and `a is None`→regime i. Unit-checked all three regimes + the
   equality boundaries. OPTIMISTIC keeps the strict rule: `IdealModel.on_trade` fires on `yp > offer`
   (models.py) and only records the regime for information.
4. **FIRST_RUN reproducible — PASS.** Reran `python -m sim.v32_replay.lab` over the 4 real journals
   (out under the review worktree). Calibration reproduced exactly: 4 windows / 303 trades / 303 eval,
   spot agreement 85.5%, cap error -2.26/-6.00/2.00c, regimes 18.8/0/81.2%, P(fill) maker 6.9% vs strict
   6.6% (21/20), factor 1.050 (p10 1.050), print size 22.0/1.0, B -0.23/+0.98c, replaces 88 vs 77.
   Estimates reproduced: OPT +10.2 / BASE +11.2 / PESS +9.2c (1 fill each). (Forward table not re-run —
   needs the range loader + `historical-data/tob`; its calibration inputs all reproduce.)
5. **"Cap never binds on the forward set" is MEASURED, not assumed — PASS.** The cap shift is applied in
   `run_forward` as `cap = cap + cap_shift` before `solve_n` (forward.py). `build_forward_estimates` runs
   a `cap_only` pass (strict rule + mean shift) alongside `opt` (no shift) and emits the `cap_effect`
   delta table (Δfills, Δmean_c) per E — the FIRST_RUN "+0 / +0.0c" is that machine-computed comparison,
   i.e. the claim is backed by a measured pass, not asserted.
6. **Fill-factor caveat is stated — PASS.** README, forward.py docstring, `render_forward` note, and
   FIRST_RUN §Bottom-line all state the maker-rule fill factor is carried from the journals onto the
   forward set (which lacks ms bucket books, so the rule cannot be re-evaluated there).
7. **Memory — PASS.** `iter_frames` streams one record at a time (generator); `run_window` processes one
   journal per iteration; the engine folds books/deques (12s strike history, popped) and never retains
   raw frames; 15M books are counted not folded. Per-window metric lists grow with ticks (aggregates,
   not raw frames) and are released between windows.

## Findings (one line each)

- FIXED (defect): estimates.py:5 docstring said BASE counts "BOOK-SWEPT fills" — retired; it counts
  spread-aware maker-rule fills. Corrected.
- FIXED (defect): estimates.py:7 docstring said PESSIMISTIC drops "< 2 lots"; code drops < 1 lot
  (matches README/FIRST_RUN/report footnote). Corrected.
- FIXED (dead code): estimates.py:29 `MIN_THROUGH_PRINT_LOTS = 2` was unused and misleading; replaced
  with `PESSIMISTIC_MIN_PRINT_LOTS = 1`, now used at the drop site (no behavior change).
- FIXED (defect): forward.py:16-18 docstring said BASE/PESS "thinned by P(swept)" and a "2-lot minimum
  print"; the code carries the maker-rule fill factor and a 1-lot floor. Corrected.
- FIXED (defect): calibration.py:9-11 (c) docstring described the retired "sweep haircut / P(swept)";
  it now measures the maker-vs-strict regime split + fill factor. Corrected.
- Added test `test_pessimistic_drops_sub_1_lot_prints_only` pinning the 1-lot floor (kept at 1, dropped
  at 0.5; BASE keeps the sub-lot fill). Package tests 22 → 23, all pass.
- NOTE (not fixed, non-blocking): `forward._RANGE_DIR` hardcodes a specific session scratchpad id;
  overridable via `--range-loader`, and the forward apply is opt-in. Fine as a research default.
- NOTE (not fixed, benign): end-of-window fills whose +1.5s completion never arrives fall back to the
  current strike top (strike history trimmed to ~12s) in `_asof_strike`; only affects a fill in the last
  1.5s of a window, and normal fills resolve promptly via `_drain_pending` with correct as-of history.

## Tests

- `python -m pytest -q sim/v32_replay/tests` → **23 passed** (was 22; +1 regression test).
- `cd pilot && python -m pytest -q` → **819 passed, 2 skipped, 2 errors**. The 2 errors are
  `tests/test_quintile.py` FileNotFoundError on `historical-data/15-minute/...` files absent in the
  review worktree — a data-availability artifact, NOT this PR. The PR's own two commits touch only
  `sim/v32_replay/` (`git diff --name-only 2db63c7~1 3574397` → `sim` only); no pilot file is modified.
  (`git diff --stat 89c3a4e` also lists pilot files, but only because the branch is based on `42d3609`,
  before #43/#44 merged to main — those show as reversals, not PR changes.)
