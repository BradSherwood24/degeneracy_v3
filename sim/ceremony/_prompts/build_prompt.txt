You are the BUILDER for degeneracy_v3's Rung 1 census/sim, running on Opus 4.8 per project mandate. Ceremony: commission -> design review (done) -> build (YOU) -> adversarial review -> one frozen run by the bridge. You build exactly what the spec says; every deviation is a written confession in your build report, never a silent choice.

READ FIRST, in order (repo-relative; you are in the repo root):
1. sim/ceremony/rung1_commission.md — the frozen spec INCLUDING AMENDMENT A2 (§10), which supersedes conflicting earlier text.
2. sim/ceremony/rung1_design_review.md — the review whose F1-F9 findings A2 adopts; it also documents verified data facts (field names, strike_type semantics, expiration_value, fee goldens) you should rely on.

BUILD under sim/ (python 3.12, stdlib + whatever of pandas/numpy is already installed — check first; if neither is installed, pure stdlib is fine and preferred over installing anything):
- sim/loader.py — day-file JSONL loading for historical-data/{15-minute,1-hour}/{markets,candles}; day assignment per A2.7 (close_time ISO -> epoch -1s -> UTC date); dedupe by ticker with byte-identical assertion; and THE SEAL: acknowledge_sealed_read=False parameter, raising SealError on any access to UTC days 2026-08-02..2026-08-18 unless True. The sealed date list is HARDCODED, not configurable.
- sim/census.py — builds the per-hour census rows per the commission: pair construction (nearest threshold, hole G, H/L lines), A2.2 epsilon exclusion, outcomes solely from result fields with the A2.3 expiration_value integrity cross-check, economics per section 5 (BASE and WORST columns, T-5min primary snapshot indexed by end_period_ts == T-300s, descriptive snapshots T-13/T-10/T-2, fee = ceil(0.07*p*(1-p)*10000)/10000 per leg on the traded side's own price), sigma-hat per A2.1/A2.4, NO-PAIR and exclusion receipts per section 2/7. Output: sim/out/census_train.csv + sim/out/census_receipt.json (row counts, exclusion inventory by reason, sha256 of every input file consumed).
- sim/gate_fit.py — quintile bucketing on G/sigma (train boundaries), per-bucket n / pin rate / mean C both columns / EV/pair, day-clustered bootstrap 95% CI (resample days with replacement, 10,000 draws, seed 26), the largest-contiguous-from-low-end gate rule, output sim/out/gate.json + sim/out/gate_report.md carrying the A2.6 descriptive-CI sentence verbatim.
- sim/unseal_runner.py — WRITTEN BUT NEVER RUN: applies a frozen gate.json to the sealed 17 days (acknowledge_sealed_read=True lives ONLY here), computes the falsifier quantities (it reads the falsifier clauses from sim/ceremony/falsifier.md at runtime; that file does not exist yet and the runner must refuse to start without it), emits aggregates only. Refuse to run if gate.json is missing or if census_train artifacts are absent.
- sim/tests/ — pytest: the pin truth table including exact-boundary prints in BOTH orientations (A2.3), the four fee goldens as LITERALS (0.57->0.0172, 0.46->0.0174, 0.24->0.0128, 0.11->0.0069), complement identity (NO ask = 1 - yes_bid), seal refusal (loader raises without acknowledge; unseal_runner refuses without falsifier.md), dedupe byte-identity, day-assignment boundary (00:00:00 UTC belongs to prior day), sigma-hat count/stitching (9 anchors, cross-day backward), G<$0.01 exclusion, NaN hard-fail.

SMOKE RUN (the only data run you are allowed): run the census end-to-end on AT MOST 5 train days (pick 2026-07-14..2026-07-18), verify receipts, and hand-reconcile ONE hour against the design review's verified 2026-07-15 08:00 example (C_base=0.7239, C_worst=0.8564). Do NOT run the full train census — that is the bridge's frozen run. Do NOT touch any file dated 2026-08-02..2026-08-18 by ANY means (the harness also denies them). Do NOT create or modify anything under sim/ceremony/ except your build report.

HOUSE LAW: registered specs rule — where the spec pins a value (dates, seed 26, quintiles, T-5 primary, epsilon $0.01, fee formula), enforce it with refusals in code, not comments. Never place orders; the proxy at 127.0.0.1:8642 is not needed for this task at all (all data is local). Report receipts (counts, shas, paths, measured values), not assurances.

DELIVERABLE: write sim/ceremony/rung1_build_report.md — files created, test results (paste the pytest tail), the smoke-run receipts, the hand-reconciliation, and a CONFESSIONS section listing every judgment call or deviation, however small (empty confession sections are treated as a red flag by the adversarial reviewer, who is instructed to find what you did not confess). Your final stdout message: a 10-line summary ending with BUILD-COMPLETE or BUILD-BLOCKED plus reasons.